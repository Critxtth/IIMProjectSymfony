{% extends 'base.html.twig' %}

{% block main %}
    <div class="my-5">
        <div class="container">
            <div class="row my-5">
                <div class="col">
                    <a href="{{ path('product') }}" class="btn btn-success">Back to all products</a>
                </div>
            </div>
            <div class="row">
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header">
                           {{ product.name }}
                        </div>
                        <div class="card-body">
                            Price : {{ product.price }} €
                        </div>
                        <div class="card-footer">
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}